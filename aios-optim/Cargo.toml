[package]
name = "aios-optim"
version = "1.0.0"
edition = "2021"

[dependencies]
aios-core = { path = "../aios-core" }
serde = { version = "1", features = ["derive"] }
bincode = "1"
log = "0.4"

[dev-dependencies]
